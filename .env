# Port this proxy server listens on
PORT=3333

# Base URL of the real upstream API to forward requests to
BASE_URL=https://app-<api>-api-test.azurewebsites.net

# Set to true to log the full Authorization header value (default: redacted)
LOG_AUTH=false
