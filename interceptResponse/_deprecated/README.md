These files are kept for reference. The auto-loader in `src/rules.ts` skips any
folder or file whose name starts with `_`, so nothing in here runs.

Use the generalized replacer interceptor (JSON files in `/replacers/`) instead.
